#create a variable to store the input

num = input()

# create two variables that store the value of index 0 and -1 as integersw 
first_digit = int(num[0])
last_digit = int(num[-1])

#add the two variables
sum = first_digit + last_digit

#prints the sum
print(sum)

